import { Component } from "@angular/core";

@Component({
    templateUrl: "sorry-page.template.html",
    moduleId: module.id
})

export class SorryPageComponent {
    private message: string = "Sorry. Page not found";
}